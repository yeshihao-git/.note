---
tags:
  - 前端
  - js
---
# 深拷贝函数

**what**：
递归复制对象的所有层级，在内存中生成一个与原对象完全独立的新副本

**why**：
在 JavaScript 中，基本类型（数字、字符串等）是按值传递的，但**引用类型（对象、数组）是按地址传递的**
```js
let objA = { name: '张三', info: { age: 20 } };
let objB = objA; // 只是复制了内存地址
objB.name = '李四';
console.log(objA.name); // '李四' —— 原数据被污染了！
```