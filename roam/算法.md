---
tags:
  - 面试
---
# 算法
## 移动操作

```cpp
int n = 10;
// NOTE 此操作不会修改 n 的值（推荐）
for (int i = 0; i < n; ++i)     // 执行 n 次
for (int i = 0; i < n-1; ++i)   // 执行 n-1 次

// HACK 此操作会修改 n 的值！！！！（后续代码若用到n，则易出现问题）
while (n--)                     // 执行 n 次
while (--n)                     // 执行 n-1 次
```
- [LeetCode 19. 删除链表的倒数第 N 个结点](https://leetcode.cn/problems/remove-nth-node-from-end-of-list/description/) 
- [LeetCode 92. 反转链表 II](https://leetcode.cn/problems/reverse-linked-list-ii/description/) 
- [LeetCode 25. K 个一组翻转链表](https://leetcode.cn/problems/reverse-nodes-in-k-group/description/)

## 判断四象限

```cpp
// 涵盖以下3种情况
// 1. p空 q空
// 2. p空 q不空
// 3. p不空 q空
if (p == nullptr || q == nullptr) {
    return p == q;        // 1. p空 q空
}

// 4. p不空 q不空
```
- [LeetCode 101. 对称二叉树](https://leetcode.cn/problems/symmetric-tree/description/)

## 快速排序

```cpp
void quick_sort(int q[], int l, int r) {
  if (l >= r) return;
  int i = l - 1, j = r + 1, x = q[l+r>>1];
  while (i < j) {
    do ++i; while (q[i] < x);
    do --j; while (q[j] > x);
    if (i < j) swap(q[i], q[j]);
  }
  quick_sort(q, l, j), quick_sort(q, j+1, r);
}
```
- [LeetCode 912.排序数组](https://leetcode.cn/problems/sort-an-array/description/)
- [LeetCode 215.数组中的第K个最大元素](https://leetcode.cn/problems/kth-largest-element-in-an-array/description/)
- [BM47 寻找第K大](https://www.nowcoder.com/practice/e016ad9b7f0b45048c58a9f27ba618bf?tpId=295&tqId=44581&sourceUrl=%2Fexam%2Foj%3FquestionJobId%3D10%26subTabName%3Donline_coding_page)

## 归并排序
1. 找中间节点
2. 分治
3. 合并
```cpp
void merge_sort(int q[], int l, int r) {
   if (l >= r) return;
   int mid = (l+r)>>1;
   merge_sort(q, l, mid), merge_sort(q, mid+1, r);

   int k = 0, i = l, j = mid + 1;
   while (i <= mid && j <= r)
        if (q[i] <= q[j]) tmp[k++] = q[i++];
        else tmp[k++] = q[j++];

   while (i <= mid) tmp[k++] = q[i++];
   while (j <= r) tmp[k++] = q[j++];

   for (int i = l, j = 0; i <= r; ++i, ++j) q[i] =tmp[j];
}
```
https://www.acwing.com/problem/content/789/

##  二分查找

```cpp
// 闭区间写法
int l = 0, r = nums.size()-1;
while (l <= r) {
  int mid = (l + r) >> 1;
  if (/*满足某种性质*/) {
    //...
  } else {
    //...
  }
 }
```
- [LeetCode 33.搜索旋转排序数组](https://leetcode.cn/problems/search-in-rotated-sorted-array/description/) 
- [LeetCode 34.在排序数组中查找元素的第一个和最后一个位置](https://leetcode.cn/problems/find-first-and-last-position-of-element-in-sorted-array/description/)

## 定长滑动窗口

```cpp
for (int l = 0, r = 0; r < n; ++r) {
    /* 具体逻辑 */
    if (r-l+1 == k) {/* 具体逻辑 */}
    /* 具体逻辑 */
 }
```
- [LeetCode 1456.定长子串中元音的最大数目](https://leetcode.cn/problems/maximum-number-of-vowels-in-a-substring-of-given-length/description/) 
- [LeetCode 438.找到字符串中所有字母异位词](https://leetcode.cn/problems/find-all-anagrams-in-a-string/description/?envType=study-plan-v2&envId=top-100-liked) 

## 不定长滑动窗口

```cpp
for (int l = 0, r = 0; r < n; ++r) {
    /* 具体逻辑 */
    while (/* 具体逻辑 */) {/* 具体逻辑 */}
    /* 具体逻辑 */
 }
```
- [LeetCode 3.无重复字符的最长子串](https://leetcode.cn/problems/longest-substring-without-repeating-characters/description/?envType=study-plan-v2&envId=top-100-liked) 

## 前缀和

```cpp
// NOTE 计算前缀和
// NOTE s[0] = 0
for (int i = 0; i < nums.size(); ++i) s[i+1] = s[i] + nums[i];

// NOTE 计算子数组和：下标l到r的子数组和
s[r+1] - s[l]
```
- [LeetCode 303.区域和检索 - 数组不可变](https://leetcode.cn/problems/range-sum-query-immutable/description/) 
- [LeetCode 53.最大子数组和](https://leetcode.cn/problems/maximum-subarray/description/) 
- [AcWing 797.差分](https://www.acwing.com/problem/content/description/797/) 

> **推导**：
> 数组a[n]
> 前缀和数组s[i]定义为数组a[i]之前数的和：a[0] + a[1] + ... + a[i-1]
> s[i+1] = s[i] + a[i]
> 计算子数组和：下标l到r的子数组和
> s[r+1] - s[l]

## 二维矩阵

```cpp
const int DIRS[4][2] = {{0,1},{1,0},{0,-1},{-1,0}}; // 4个方向
int di = 0;                                         // 当前方向

int i = 0, j = 0;                                   // 当前位置
int x = i + DIRS[di][0];                            // 横坐标下一步
int y = j + DIRS[di][1];                            // 纵坐标下一步

if (x < 0 || x >= m || y < 0 || y >= n || matrix[x][y] == INT_MAX) {  // 判断是否越界
  di = (di + 1) % 4;                                // 变向
}
```
- [LeetCode 54.螺旋矩阵](https://leetcode.cn/problems/spiral-matrix/description/)

## 链表中间节点

```cpp
// 奇数返回中间节点；偶数返回中间右边节点
// 无需头节点
ListNode* middleNode(ListNode* head) {
   ListNode *f = head, *s = head;
   while (f && f->next) {
    f = f->next->next;
    s = s->next;
   }
   return s;
}
```
- [LeetCode 143.重排链表](https://leetcode.cn/problems/reorder-list/description/) 
- [LeetCode 876.链表的中间节点](https://leetcode.cn/problems/middle-of-the-linked-list/description/)

## 链表反转

```cpp
/* 无需头节点 */
class Solution {
public:
    ListNode* reverseList(ListNode* head) {
        ListNode* p = nullptr, c = head;
        while (c) {
            ListNode* n = c->next;
            c->next = p;
            p = c;
            c = n;
        }
        return p;
    }
};
```
- [LeetCode 143.重排链表](https://leetcode.cn/problems/reorder-list/description/) 
- [LeetCode 206.反转链表](https://leetcode.cn/problems/reverse-linked-list/description/)

## 链表两数相加

```cpp
ListNode* addTwo(ListNode* l1, ListNode* l2) {
    ListNode* p1 = l1, *p2 = l2, *dummy = new ListNode(-1), *cur = dummy;
    int carry = 0;
    while (p1 || p2 || carry) {
        if (p1) {
            carry += p1->val;
            p1 = p1->next;
        }
        if (p2) {
            carry += p2->val;
            p2 = p2->next;
        }
        cur->next = new ListNode(carry%10);
        cur = cur->next;
        carry /= 10;
    }
    return dummy->next;
}
```

## 合并有序链表

```cpp
ListNode* mergeTwoLists(ListNode* l1, ListNode* l2) {
    ListNode* dummy, *cur = dummy;
    while (l1 && l2) {
        if (l1->val < l2->val) {
            cur->next = l1;
            l1 = l1->next;
        } else {
            cur->next = l2;
            l2 = l2->next;
        }
        cur = cur->next;
    }
    cur->next = l1 ? l1 : l2;
    return dummy.next;
```
- [LeetCode 21.合并两个有序链表](https://leetcode.cn/problems/merge-two-sorted-lists/description/) 

## 树形DP

104.二叉树的最大深度 （这是很多题的基础！）
```cpp
/* NOTE 树形DP：原问题拆解为多个子问题，找到原问题和子问题之间的关系 */
class Solution {
public:
    int maxDepth(TreeNode* root) {     // 原问题 ：以当前节点为根的二叉树的最大深度
       if (root == nullptr) return 0;
       int l = maxDepth(root->left);   // 子问题1：左子树的最大深度
       int r = maxDepth(root->right);  // 子问题2：右子树的最大深度
       return max(l, r) + 1;           // 原问题和子问题的关系：原问题 = max(子问题1，子问题2) + 1
    }
};
```

543.二叉树的直径
```cpp
class Solution {
public:
    int diameterOfBinaryTree(TreeNode* root) {
        int ans = 0;

        function<int(TreeNode*)> dfs = [&](TreeNode* root) {
            /* 104.二叉树的最大深度 */
            if (root == nullptr) {
                return 0;
            }

            /* 枚举当前节点，更新状态 */
            int l = dfs(root->left);
            int r = dfs(root->right);
            ans = max(ans, l+r);

            /* 104.二叉树的最大深度 */
            return max(l, r) + 1;
        };

        dfs(root);
        return ans;
    }
};
```
- [LeetCode 104.二叉树的最大深度](https://leetcode.cn/problems/maximum-depth-of-binary-tree/description/) 
- [LeetCode 543.二叉树的直径](https://leetcode.cn/problems/diameter-of-binary-tree/description/) 
- [LeetCode 124.二叉树中的最大路径和](https://leetcode.cn/problems/binary-tree-maximum-path-sum/description/)

## 用栈模拟二叉树的前/中序遍历

```cpp
vector<int> traversal(TreeNode* root) {
  stack<TreeNode*> st;
  while (root || !st.empty()) {     // 表示当前还有节点需要处理（节点存在或栈非空）
    while (root) {
      // 节点入栈，向左遍历相关逻辑
    }
    // 节点出栈，向右遍历相关逻辑
  }
}
```
- [LeetCode 144.二叉树的前序遍历](https://leetcode.cn/problems/binary-tree-preorder-traversal/description/) 
- [LeetCode 94.二叉树的中序遍历](https://leetcode.cn/problems/binary-tree-inorder-traversal/description/)

## 字符串拆分

版本1
```cpp
string s;
int n = s.size();
for (int i = 0; i < n; ++i) {
  int j = i;
  while (j < n && s[j] != ' ') ++j;
  // 拆分逻辑
  i = j;
}
```

版本2
```cpp
vector<string> str_split(string s) {
  s += " ";
  string w;
  vector<string> ws;
  for (char c:s) {
      if  (c == ' ') {
          if (!w.empty()) {
              ws.push_back(w);
              w.clear();
          }
      } else {
          w += c;
      }
  }
}
```
- [LCR 181.字符串中的单词反转](https://leetcode.cn/problems/fan-zhuan-dan-ci-shun-xu-lcof/description/) 

## 合并区间

1. 将区间按左端点排序
2. 合并区间
```cpp
typedef PII = pair<int, int>
void merge(vector<PII> &segs)
{
    vector<PII> res;

    sort(segs.begin(), segs.end());

    int st = -2e9, ed = -2e9;
    for (auto seg : segs)
        if (ed < seg.first)
        {
            if (st != -2e9) res.push_back({st, ed});
            st = seg.first, ed = seg.second;
        }
        else ed = max(ed, seg.second);

    if (st != -2e9) res.push_back({st, ed});

    segs = res;
}
```
- [AcWing 805](https://www.acwing.com/problem/content/805/) 
- [BM89 合并区间](https://www.nowcoder.com/practice/69f4e5b7ad284a478777cb2a17fb5e6a?tpId=295&tqId=691&sourceUrl=%2Fexam%2Foj%3FquestionJobId%3D10%26subTabName%3Donline_coding_page) 

## 双指针

1. 暴力写法
2. 看有没有单调性，然后优化暴力写法
```cpp
// 一维序列、二维序列都能用；以下只是大致的代码，主要看思想
for (int i = 0, j = 0; i < n; ++i) {
  while (j < i && check(i, j)) j++;
  // 具体问题逻辑
 }
```
- [AcWing 801](https://www.acwing.com/problem/content/801/) 
- [AcWing 802](https://www.acwing.com/problem/content/description/802/) 
- [BM92 最长无重复子数组](https://www.nowcoder.com/practice/b56799ebfd684fb394bd315e89324fb4?tpId=295&tqId=1008889&sourceUrl=%2Fexam%2Foj%3FquestionJobId%3D10%26subTabName%3Donline_coding_page) 

## 回溯

选哪个
```cpp
void backtrack(参数列表) {
    // 1. 终止条件：达到目标状态（如收集完所有解）
    if (终止条件) {
        保存当前解到结果集;
        return;
    }

    // 2. 遍历所有可能的选择（根据问题范围确定遍历起点和终点）
    for (选择范围内的元素) {
        // 3. 剪枝：排除不符合条件的选择（可选，优化效率）
        if (剪枝条件) continue;

        // 4. 做出选择：将当前元素加入路径
        路径.push_back(当前元素);

        // 5. 递归：深入下一层搜索
        backtrack(更新后的参数);

        // 6. 回溯：撤销选择，恢复状态（关键步骤）
        路径.pop_back();
    }
}
```

选或不选

## BFS

```cpp
void bfs(TreeNode* root) {
  queue<TreeNode*> q;                     // 1. 队列
  q.push(root);                           // 2. 放入第1个节点
  while (!q.empty()) {                    // 3. 队列不空时循环
    auto node = q.front();                // 4. 取出节点进行处理
    /* 对节点具体处理逻辑 */
    if (node->left) q.push(node->left);   // 5. 将左右孩子加入队列
    if (node->right) q.push(node->right);
    q.pop();                              // 6. 弹出节点
  }
}
```
- [LeetCode 102.二叉树的层序遍历](https://leetcode.cn/problems/binary-tree-level-order-traversal/description/) 
- [LeetCode 662.二叉树最大宽度](https://leetcode.cn/problems/maximum-width-of-binary-tree/description/) 
- [LeetCode 958.二叉树的完全性检验](https://leetcode.cn/problems/check-completeness-of-a-binary-tree/description/)